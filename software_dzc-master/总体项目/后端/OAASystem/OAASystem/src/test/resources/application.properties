# Test AI API Configuration
ai.api.key=test-api-key
ai.api.url=https://api.test.com/v1

# Test Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/cmh?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Dzc15040361355

# MyBatis Configuration
mybatis.config-location=classpath:/mybatis-config.xml
mybatis.type-aliases-package=edu.neu.oaas.pojo
mybatis.mapper-locations=classpath:mapper/*.xml

# Logging Configuration
logging.level.edu.neu.oaas.mapper=DEBUG
logging.level.org.springframework.test=DEBUG
logging.level.org.springframework.transaction=DEBUG

# Transaction Configuration
spring.transaction.default-timeout=30
spring.transaction.rollback-on-commit-failure=true

# Test Configuration
spring.test.database.replace=none
spring.main.allow-bean-definition-overriding=true

# Visitor Counter Configuration for Tests
visitor.counter.file=classpath:visitor-counter-test.txt 